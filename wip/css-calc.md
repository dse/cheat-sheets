# css calc()

```
+   addition
        must be surrounded by whitespace.
        no whitespace after means unary plus.
-   subtraction
        must be surrounded by whitespace.
        no whitespace after means unary minus.
*   multiplication
        at least one of the arguments must be a number.
/   division
        the divisor must be a number.

```
