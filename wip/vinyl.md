# Vinyl

```
.stat                   file stat object or null
.contents               stream, buffer, or null
.path
.cwd
.base
.isBuffer()
.isStream()
.isNull()
.isDirectory()
.isSymbolic()
.clone()

getters/setters
    .contents
    .cwd
    .relative
    .dirname
    .basename
    .stem               filename without suffix
    .extname            e.g., '.txt'
    .path
```
